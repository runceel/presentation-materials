# `design-adr.md` 編集メモ

`design-adr.md` を編集するときに、発表の論旨・トーン・構成を維持するためのメモです。

## この LT で伝えたいこと

- 設計書には現在の仕様だけを書く。
- 過去の設計判断は ADR に残す。
- AI には設計書と ADR の両方を読ませる。
- ADR は新しい手法ではないが、AI に作成・更新を支援してもらうことで継続しやすくなる。

## 発表スタイル

- Microsoft テーマを使用する。
- 1 スライド 1 メッセージを基本にする。
- 具体的な失敗・試行錯誤から入り、最後に考えをまとめる。
- 長い説明はトークで補い、スライドへ詰め込みすぎない。
- 未検証のことは断定せず、課題や限界も残す。

## 構成

1. AI が過去の経緯を混ぜたドキュメントやコードコメントを書く問題
2. 最新情報だけを残すルールを追加した結果、AI が判断履歴を失う問題
3. ADR の導入と、設計書・判断履歴の分離
4. ADR の効果、チーム開発での課題、増えすぎた場合の検索性
5. AI によって ADR を継続しやすくなったというまとめ

## 編集時の注意

- 「設計書は最新だけ」「判断履歴は ADR」という二つを明確に書き分ける。
- 具体例は実在の非公開システムを特定できない形で扱う。
- ADR は判断、背景・理由、却下した代替案、影響・トレードオフを残すものとして説明する。
- 採用済み ADR は原則上書きせず、判断変更時は新しい ADR から `Superseded` にする。
- 表紙と締めの構成、スライド区切り `---`、コードフェンスの言語指定を維持する。
