# `design-adr.md` 編集メモ

このファイルは、LT スライド `design-adr.md` を今後編集するときに、作成経緯・話したい内容・トーン・根拠資料を失わないためのメモです。

## この LT で伝えたいこと

AI にコードや設計書を書いてもらうと、現在の仕様だけでなく、過去の実装・変更理由・採用しなかった案まで成果物へ混ざることがある。

そこで次のように情報を分ける。

- 設計書には **現在の仕様だけ**を書く
- 過去の設計判断は **ADR** に残す
- AI には **設計書と ADR の両方**を読ませる

ADR 自体は新しい手法ではない。AI に作成・更新を支援してもらうことで、人間だけでは継続が難しかった記録を現実的な工数で残せるようになり、AI 自身も過去に却下した案を繰り返しにくくなる、という話。

## 発表スタイル

- Microsoft テーマを使用する
- 表紙の kicker は **ARCHITECTURE DECISION RECORD**
- 高橋メソッドほど極端にはしないが、**1 スライド 1 メッセージ**を基本にする
- スライドを読ませるのではなく、話しながら短い間隔でめくる
- 長い説明はトークで補い、スライドへ詰め込まない
- 具体的な失敗・試行錯誤から入り、最後に考えをまとめる
- 抽象的な標語や「意識が高い」表現は避ける
- 断定しすぎず、未検証のことは「まだ試せていません」と正直に言う
- 「ということで」「正直」「私はしょっちゅうありました」のような、普段の話し言葉を残す
- 意味が曖昧なまとめを避ける。「AI にはその両方」のように指示語だけで済ませず、対象を明記する

トーンの参考は `D:\Repos\runceel\zenn\articles`。特徴は「試してみた → 良かった → 別の問題が出た → こうした」という流れ、具体的な観察、控えめなユーモア、限界の明記。

## 現在のデッキ

- ソース: `design-adr.md`
- 現在: **35 スライド**
- テーマ: `microsoft`
- 表紙: `layout: title`
- 最終ページ: 背表紙（`layout: backcover`、拡張機能が自動付与）
- プロフィール画像: `/assets/profile.jpg`
- X: `@okazuki`
- Zenn: `https://zenn.dev/okazuki`

### 現在の流れ

1. 表紙
2. 自己紹介
3. 過去の経緯まで混ざった SharePoint Indexer の設計書例
4. 現在の仕様だけを書いた「こうあってほしい設計書」
5. AI に書かせると過去の経緯まで残る
6. 「私はしょっちゅうありました」
7. ドキュメント用 SKILL に最新情報だけ残すよう指示
8. コードコメントにも過去の経緯が残る例
9. コード用 SKILL も追加
10. 今度は AI が同じ間違いを繰り返す
11. Bicep のタイミング問題
12. 一度は回避
13. その後も同じ間違い
14. AI がエラー対応で迷走
15. トークンを無駄に消費
16. ADR を導入
17. Architecture Decision Record の説明
18. 重要な設計判断を決まった形で残す
19. ADR に残す内容
20. AI に渡したものはテンプレートと追加ルール
21. SKILL とテンプレート例
22. 前にやめた方法を繰り返しにくくなった
23. 設計書には最新状態だけが残るようになった
24. 公開リポジトリに ADR が 18 件ある例
25. ADR-0017 の具体例
26. 対応する現在仕様の設計書
27. ADR が Superseded になった具体例
28. PR レビューで ADR が判断材料になる
29. チーム開発では ADR 同士が衝突する可能性
30. 全体を俯瞰する人が必要かもしれない
31. ADR 検索用 CLI などは未検証
32. まとめ
33. ADR は昔からあるが AI で続けやすくなった
34. ADR を試してほしいケース
35. クロージング

## トークスクリプトの原案

以下が、ユーザーが提示した話したい内容の原案。今後スライドを編集しても、この論旨と順序を壊さないこと。

> 「AI が設計書を歴史書にする前に ― 現在の仕様と判断履歴を分けるコンテキスト設計」というタイトルで LT をさせていただきます。
>
> 自己紹介ですが、マイクロソフトでクラウドソリューションアーキテクト兼エヴァンジェリストをしています。大田一希です。
>
> さて、さっそく本題に入っていこうと思うのですが、皆さんは AI にドキュメントやコードを書いてもらったときに、AI が過去の経緯を混ぜて変なコメントを残したり、ドキュメントに過去の経緯まで混ぜて書いてしまったことはありませんか？
>
> 私はしょっちゅうそういう問題にあってしまって、なるべく最新の情報のみ残すようにと強くお願いしていました。例えばドキュメントを書くときに読み込まれる SKILL に、「ドキュメントには過去の経緯を含めずに最新の情報だけを残してください」といった注意書きを入れたりしていました。
>
> そうしてもコードのコメントに不要な過去の経緯を書いたりしていたので、コードの方にもそういうことは書かないようにという SKILL を別途用意していました。
>
> ただ、そうすると今度は開発を進めていくうちに、AI が何度も同じ過ちを犯すということが増えてきました。
>
> 例えば、この間開発していたアプリで Azure の Bicep、IaC をするためのものを書いていたときに、ちょっとしたタイミング問題でエラーが起きていました。これは AI に指示をして回避していたのですが、そのあと何回も Bicep をいじるときに同じ間違いをして、そのたびに AI がエラー対応のために迷走するということが起きていました。正直、トークンを無駄に消費していました。
>
> そこで、そのあとに開発するプログラムでは ADR を導入することにしました。
>
> ADR は Architecture Decision Record というもので、新しい概念ではなく、昔からあるドキュメントを書く際のベストプラクティス的なものです。実際に Microsoft、AWS、Google でも紹介されていて、アーキテクチャに関する重要な要件について、除外した代替案を含むすべての重要な決定を一貫したフォーマットで記録したものです。
>
> AI に対して ADR のテンプレートと、重要な設計変更などがあるときに ADR を追加するよう指示することで、過去にやらないと判断したことを繰り返さないという効果が得られました。そしてドキュメントの方は、ほぼすべて最新の状態のみが記載されるようになりました。
>
> 実際に ADR の SKILL やテンプレートは、こんな感じで必要に応じて記録されていっています。
>
> ADR を導入すると、Pull Request をレビューする際にも ADR があることで、「何か設計に関して重要な判断をしたのかな？」ということが一目でわかります。少ししっかり見ないといけないのかな、という判断材料になるのも良かったです。
>
> ただ、課題として感じているのが、チームで開発している際に ADR の判断のコンフリクトが起こらないようにマージしていくには、誰かが全体を俯瞰して見る必要があることです。ADR が巨大になったときの扱いについても課題が残ると思っています。
>
> もしかしたら ADR を効率的に検索するための CLI やスクリプトなども別途用意しておいた方が、大規模開発では良いケースがあると思っていますが、そこまではまだ試せていません。
>
> ということで、今日のまとめです。
>
> 今回は、設計書を最新の情報のみに保ちつつ、過去の設計判断を ADR として残すことで、AI に同じ過ちをさせないということについてお話ししました。
>
> ADR を残すということは新しい方法ではないですが、人間がやるとなると工数がかかって、完璧にやるのは難しかったと思います。AI の登場により現実的な工数で実施できて、しかも ADR がもともと提供していたメリットを AI が享受して、同じ間違いをしにくくなるという効果がありました。
>
> AI が過去と同じ過ちを繰り返す問題を持っていたり、AI が残すドキュメントが過去の経緯まで含んだ歴史書みたいなものになって困っていたりする場合は、ADR を試してみていただければと思います。
>
> ということで以上となります。ご清聴ありがとうございました。

## 現在のスライドに合わせたトーク上の補足

### 3 ページ目: 経緯まで混ざった設計書

ここでは SharePoint Indexer が何をするものかを短く説明してから、現在仕様の後に変更履歴が続いていることを指摘する。

話し方の例:

> これは SharePoint にあるファイルを定期的に取得して、AI Search に登録する Indexer の例です。今の仕様は「フォルダーを順番にたどって、50 フォルダーごとに処理を区切る」です。ただ、その後に「以前は全部を一度に取っていた」「10 分でタイムアウトした」「さらに実行履歴が大きくなった」と、今の動きを知るだけなら不要な経緯まで続いています。

### 4 ページ目: こうあってほしい設計書

3 ページ目と同じ現在仕様だけを残し、変更理由を消した対比ページ。

話し方の例:

> 私が設計書に残したかったのは、これだけです。今どう動くかだけが分かればよくて、どうしてそこにたどり着いたのかは別の場所に置きたいと思っていました。

### Bicep の章

「AI が同じ間違いを繰り返した」実体験は **Bicep のタイミング問題**。SharePoint Indexer の履歴を、同じミスの証拠として扱わないこと。

SharePoint Indexer から確認できるのは、短期間に設計変更が重なり、その理由がコード・ドキュメント・コミットへ分散したことまで。AI が同じミスを繰り返した因果関係はコミット履歴だけでは証明できない。

## 根拠にしたリポジトリ

### 話し方のトーン

- `D:\Repos\runceel\zenn`
- 特に `articles` 配下の記事を参照した

### SharePoint Indexer の例

- `D:\Repos\kaota_microsoft\honda-taro-poc`
- 非公開リポジトリなので、リポジトリ名・顧客名・システム固有名はスライドへ出さない

重要な根拠:

| 内容 | 根拠 |
|---|---|
| Indexer が Web / SharePoint をクロールし AI Search 登録パイプラインを担当 | `docs/application-architecture.md` の Indexer 概要 |
| SharePoint クロールをフォルダー単位 BFS へ変更し、10 分タイムアウトを回避 | commit `0153fef` |
| 50 フォルダーごとに `ContinueAsNew` し、オーケストレーション履歴をリセット | commit `a79fa14` |
| 現在の 50 フォルダー上限 | `CrawlOrchestrator.BfsFolderBatchSize` |
| 旧仕様の説明が現在ドキュメントへ混ざっている別例 | `docs/architecture/indexer-orchestration.md` |
| 失敗時にも `NoWork` を返していた旧実装の説明 | `docs/reference/indexer-runtime.md` |

### 3 ページ目の引用に関する注意

3 ページ目の SharePoint Indexer の文章は、**実際の設計書からの直引用ではない**。現在コード・設計書・上記コミット履歴を短くまとめた説明。

実際の現在ドキュメントには、「10 分タイムアウト」「50 フォルダー」「履歴肥大化」が一つの文章として並んではいない。

一度、`docs/architecture/indexer-orchestration.md` の次の直引用版も試したが、専門用語が多く、冒頭スライドとしては伝わりにくかったため不採用になった。

- 全 pending を 1 リクエストで取得
- Singleton Orchestrator
- heartbeat
- Timer 起動

ユーザーはスライド上の「実際の改修履歴をもとに再構成」という注記を不要と判断したため、現在のスライドには表示していない。ただし、今後も **直引用であるかのようには説明しないこと**。

### 公開 ADR の例

- `D:\Repos\runceel\wsl-containers-desktop`
- 公開リポジトリなので、リポジトリ名と GitHub パスをスライドへ表示してよい
- 現在 ADR-0001 から ADR-0018 まである

ADR-0017:

- Context: `ContainersViewModel` が約 1,000 行
- Decision: 共有ファサードを残し、内部責務を 4 分割
- Rejected: 画面ごとに ViewModel を分ける案。共有状態が壊れるため不採用

対応する `docs/design/containers-view.md` は、現在のスナップショットであり、経緯・検討過程を含めない方針を明記している。

### Superseded の例

- `D:\Repos\kaota_microsoft\rag-agent-chat-app`
- 非公開リポジトリ
- ADR-0002: BlobTrigger を採用
- ADR-0003: Blob 削除を検出できないため Timer 定期クロールへ変更
- 古い ADR は上書きせず、`Superseded` として残す例に使用
- スライドにはリポジトリ名やアプリ固有情報を出さず、技術的な断片だけを使う

## 事実関係

- ADR は **Architecture Decision Record** の略。`Desicion` ではなく `Decision`
- Microsoft、AWS、Google の公式ドキュメントで ADR が紹介されていることを確認済み
- ADR には判断、状況・理由、却下した代替案、影響・トレードオフを残す
- 受理済み ADR は原則として本文を上書きせず、判断を変更するときは新しい ADR から `Superseded` にする

## コード例について

8 ページ目の DataGrid / ListView の C# コメントは、過去の経緯がコードコメントへ残ることを一目で伝えるための短い例。

```csharp
// 以前はDataGridを使っていた。
// COM例外が発生したためListViewへ変更した。
var containersView = new ListView();
```

このコードを実在コードからの直引用とは説明しない。コード例の位置は、ドキュメント用 SKILL の後、コード用 SKILL の前に置く。以前はコード例の位置が後ろにあり、「それでもコードのコメントに書くので」という流れが不自然になったため、現在の順序へ変更した。

## Markdown と表示上の注意

- スライド区切りは `---`
- 表紙の front matter に次を残す

```yaml
title: AIが設計書を歴史書にする前に
kicker: ARCHITECTURE DECISION RECORD
```

- `csharp`、`markdown`、`text`、`diff` など、コードフェンスには言語名を付ける
- presentation 拡張には highlight.js 11.11.1 を同梱済み
- Mermaid は別の描画経路なので、コードハイライト対象外
- 表紙とクロージングは専用レイアウトを使う
- Microsoft テーマでは表紙の kicker に 4 色マークが付く
- Windows では、Surface Pen の末尾ボタンを 1 回押すと次へ、長押しすると前へ進み、2 回押しで外部プレゼン画面が起動 / 終了する
- Pen 操作は、Bluetooth ペアリングされたペンが生成する `Win+F20`（1 回押し）/ `Win+F19`（2 回押し）/ `Win+F18`（長押し）を presentation 拡張が受け取る

関連ファイル:

- `.github/extensions/presentation/renderer/index.html`
- `.github/extensions/presentation/renderer/renderer.js`
- `.github/extensions/presentation/renderer/slides.css`
- `.github/extensions/presentation/vendor/highlight.min.js`
- `.github/extensions/presentation/vendor/highlight.LICENSE`
- `.github/extensions/presentation/windows/pen-button-listener.ps1`

## 今後の編集で崩さないこと

- Bicep の話は「AI が同じミスを繰り返した」具体例として残す
- SharePoint Indexer の例と Bicep の問題を同じ証拠として混同しない
- 3 ページ目と 4 ページ目は、経緯入り／現在仕様だけ、という対比を維持する
- ADR の説明へ入る前に、ドキュメントの経緯を消した結果、AI が判断履歴を失ったという流れを保つ
- 「設計書は最新だけ」「判断履歴は ADR」という二つを曖昧な指示語でまとめない
- 公開・非公開リポジトリの扱いを守る
- 1 枚へ説明を詰め込みすぎない
- ユーザーの普段の話し方より格好をつけすぎない
- 課題や未検証事項を削って成功談だけにしない
- スライド数を変更したら、presentation canvas へ再登録する際の `page` / `total` をすべて更新する
- 大きな構成変更をしたら、このメモも更新する
